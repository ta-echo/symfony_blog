<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome on my blog!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
            and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
        <link rel="stylesheet" href= "{{asset('assets/css/bootstrap.min.css')}}" >
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
        <script href="{{ asset('assets//js/bootstrap.min.js') }}"></script>
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
{# Liens directes vers les pages  #}
    {# <a href="{{path('category')}}">Liste des categories</a> #}
    {# <a href="{{path('article_index')}}">Liste des articles</a> #}
    <header>
            <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <div class="container-fluid">
            <a class="navbar-brand" href="#">Navbar</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{path('home')}}">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('category')}}">Category</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('article_index')}}">Article</a>
                </li>
                   <li class="nav-item">
                    <a class="nav-link" href="{{path('nom_user_index')}}">User</a>
                </li>
            </ul>
            </div>
            </div>
        </nav>
    </header>
        {% block body %}{% endblock %}
    </body>
</html>


